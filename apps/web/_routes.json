{
  "version": 1,
  "description": "함수를 태울 경로만 고른다. HTML 경로는 geo 쿠키(미들웨어) 때문에, /api/* 는 API 라서. 나머지 정적 자산은 함수 호출 없이 CDN 이 바로 서빙한다 — 방문 1회가 자산 20여 개를 부르므로 여기 없는 경로가 함수를 타면 무료 호출 한도(10만/일)가 순식간에 샌다. 언어 랜딩을 추가하면 이 목록에도 넣을 것.",
  "include": ["/api/*", "/", "/en", "/ja", "/zh", "/es", "/pt"],
  "exclude": []
}
